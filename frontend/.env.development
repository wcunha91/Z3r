# Base da API apontando para o proxy do Vite (vite.config.ts já cria /api -> 8000)
VITE_API_BASE=/api

# Simulador do header interno (apenas em DEV). Em produção NÃO use.
VITE_INTERNAL_PROXY_HEADER=1
